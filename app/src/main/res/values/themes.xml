<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Kawaime" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/background</item>

        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/OnBackground</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>

        <item name="colorControlHighlight">@color/rippleColor</item>

        <item name="colorControlActivated">@color/main</item>
        <item name="colorControlNormal">@color/strokeColor</item>
        <item name="android:colorEdgeEffect">@color/main</item>
    </style>

    <style name="Theme.Kawaime" parent="Base.Theme.Kawaime" />
</resources>